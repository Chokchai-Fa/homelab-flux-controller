apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - infrastructure.yaml
  - image-automation.yaml
  - apps.yaml
  - rancher.yaml           # Rancher UI (independent, non-blocking Kustomization)

