apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - infrastructure.yaml
  - image-automation.yaml
  - apps.yaml
  - portainer.yaml         # Portainer UI (independent, non-blocking Kustomization)
  - code-server.yaml       # code-server IDE (independent, non-blocking Kustomization)

