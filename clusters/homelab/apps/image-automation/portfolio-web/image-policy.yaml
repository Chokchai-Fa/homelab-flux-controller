apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: portfolio-web
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: portfolio-web
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+.*$'
  policy:
    semver:
      range: '>=0.0.0-0'
