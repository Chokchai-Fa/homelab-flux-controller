apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: consumer-reminder
  app.kubernetes.io/instance: consumer-reminder
  app.kubernetes.io/component: backend
