apiVersion: v1
kind: Service
metadata:
  name: docs
  namespace: default
  labels:
    app: docs
spec:
  type: ClusterIP
  selector:
    app: docs
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
