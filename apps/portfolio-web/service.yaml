apiVersion: v1
kind: Service
metadata:
  name: portfolio-web
  namespace: default
  labels:
    app: portfolio-web
spec:
  type: NodePort
  selector:
    app: portfolio-web
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  nodePort: 30300
