---
# Portfolio Assets Ingress - rewrite /portfolio/_next/* -> /_next/* so static assets and image optimizer resolve
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portfolio-assets-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /_next/$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    # Preserve original headers for Cloudflare
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Forwarded-Proto: https";
      more_set_headers "X-Forwarded-For: $proxy_add_x_forwarded_for";
spec:
  rules:
    - http:
        paths:
          # Catch Next.js runtime/static/image requests under the /portfolio prefix and strip that prefix
          - path: /portfolio/_next(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: portfolio-web
                port:
                  number: 3000

---
# Portfolio App Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portfolio-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    # Preserve original headers for Cloudflare
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Forwarded-Proto: https";
      more_set_headers "X-Forwarded-For: $proxy_add_x_forwarded_for";
spec:
  rules:
    - http:
        paths:
          # Portfolio Next.js App - forward full path; Next.js handles basePath
          - path: /portfolio
            pathType: Prefix
            backend:
              service:
                name: portfolio-web
                port:
                  number: 3000

---
# Nginx Ingress - with rewrite
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    # Preserve original headers for Cloudflare
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Forwarded-Proto: https";
      more_set_headers "X-Forwarded-For: $proxy_add_x_forwarded_for";
spec:
  rules:
    - http:
        paths:
          # Nginx service with rewrite
          - path: /nginx(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 80
          
          # # API service (example)
          # - path: /api(/|$)(.*)
          #   pathType: ImplementationSpecific
          #   backend:
          #     service:
          #       name: api-service
          #       port:
          #         number: 8080
          
          # # Frontend service (example)
          # - path: /app(/|$)(.*)
          #   pathType: ImplementationSpecific
          #   backend:
          #     service:
          #       name: frontend-service
          #       port:
          #         number: 3000
          
          # # Database admin (example)
          # - path: /admin(/|$)(.*)
          #   backend:
          #     service:
          #       name: db-admin-service
          #       port:
          #         number: 8081
